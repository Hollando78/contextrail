node_modules/
dist/
*.log
.DS_Store
# Runtime state (pairing ledger, allowlist, audit logs, generated certs)
data/
!data/.gitkeep
# TLS material — never commit private keys / certs
certs/
*.pem
*.key
# Operator-local config (domain, cert paths, custom actions) — keep committed samples generic
config/local.config.json
config/actions.local.json
# Secrets / env (e.g. Cloudflare API token for DNS-01)
*.env
.env
src/host/public/desklet.js
src/host/public/desklet.js.map
coverage/
